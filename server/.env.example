PORT=3000
MONGO_URI=mongodb://localhost:27017/hushhh
# Generate a high-entropy secret; the server refuses to start without it.
JWT_SECRET=replace-with-a-long-random-secret
# Comma-separated browser origins. Leave unset only in local development.
CORS_ORIGINS=https://example.com
